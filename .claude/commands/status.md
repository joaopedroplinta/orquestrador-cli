---
description: Resume o que já foi implementado no orquestrador, lendo o CLAUDE.md
---

Leia o `CLAUDE.md` na raiz do projeto, especialmente a seção "Estado atual",
e resuma pro usuário:

- O que já está implementado (itens marcados `[x]`).
- O que ainda falta (itens marcados `[ ]`), na ordem em que aparecem.
- Se algum arquivo mencionado no "Estado atual" não existir mais ou tiver
  mudado de lugar, avise antes de seguir com qualquer sugestão baseada nele.

Não implemente nada — esse comando é só de leitura/resumo.
